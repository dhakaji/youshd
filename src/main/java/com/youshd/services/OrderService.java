package com.youshd.services;

public class OrderService {

//    public double getCashback(Integer userId, Integer productId) {
//        // First check if Order of given product by given user is success or not.
//        // If success calculate cashback else return 0
//        // Find all orders with affiliateUserId = userId and productId = productId. Sort on orderId and
//        List<Order> orderList = new ArrayList<Order>();
//        double cashback = 0;
//        for(Order order : orderList) {
//            if(order.getStatus().equals(OrderStatus.SUCCESS))
//                cashback += order.getDiscount() * order.getActualOrderPrice();
//        }
//        // multiply cashback into productPrice
//        return cashback;
//    }
//
//    public void returnOrder(Integer orderId) {
//        // set order status as failed
//        // If discount of current order is 20% find next order with same affiliateOrder and productId
//        // and mark discount as 20%.
//
//    }
}
